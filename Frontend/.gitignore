# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables (contains local config / keys - see .env.example)
.env
.env.local

# This file lives in Frontend/, so it can only ever match Frontend/Backend/.env,
# which doesn't exist - the actual Backend/.env is excluded by Backend/.gitignore instead.